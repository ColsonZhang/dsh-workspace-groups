/**
 * Host half for `dsh-workspace-groups`.
 *
 * The plugin is browser-only: it decorates the existing sidebar workspace list
 * with collapsible group headers and keeps the grouping in localStorage. No
 * Host service, route, or storage domain is registered here.
 */
export function apply() {}
